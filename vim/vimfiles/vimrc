call plug#begin()
Plug 'ervandew/supertab'
Plug 'jiangmiao/auto-pairs'
Plug 'justinmk/vim-sneak'
Plug 'mileszs/ack.vim'
Plug 'tomtom/tcomment_vim'
Plug 'pangloss/vim-javascript', { 'for': 'javascript' }
Plug 'vim-scripts/HTML-AutoCloseTag', { 'for': 'html' }
Plug 'duganchen/vim-soy', { 'for': 'soy' }
call plug#end()

syntax on
filetype plugin indent on
let mapleader = "\<space>"

set autoread hidden t_Co=256
set expandtab shiftwidth=2 tabstop=2 backspace=indent,eol,start
set smartcase incsearch
set guicursor=n:blinkon0
set mouse=a mousemodel=extend
set nonumber ls=2
set omnifunc=syntaxcomplete#Complete
set shortmess=atIc
set lazyredraw ttyfast ttyscroll=3 so=2
set wildmenu wildmode=longest,list,full
set path+=**
set noerrorbells vb t_vb=""
set nowritebackup noswapfile viminfo="NONE"
set nostartofline
set nowrap
set nofoldenable
set statusline=[%n]\ %F\ %=%-10.(%l,%c%V%)\ %P

runtime macros/matchit.vim

""" mappings
nn j gj
nn k gk
inoremap ii <esc>
vn < <gv
vn > >gv
nm <silent> <leader>b :ls<cr>:e #

noremap  <F1> :set hls!<cr>
inoremap <F1> <nop>
nn <F2> :set invlist!<cr>
nn <F3> :set nonu!<cr>
nn <F4> :if exists("g:syntax_on")<bar>syntax off<bar>else<bar>syntax enable<bar>endif<cr>

nn <silent> <leader>q :call wipeout#Wipeout()<cr>

set wildignore+=*.bmp,*.gif,*.ico,*.jpg,*.png,*.icov,*.pdf
set wildignore+=node_modules/*,bower_components/*
nn <leader>p :e<space>

" autocommands
" strip trailing whitespace on save
au BufWritePre * :%s/\s\+$//e
" enter automatically into the files directory
au BufEnter * silent! lcd %:p:h
" this is supposed to fix html syntax highlighting
autocmd BufEnter *.html :syntax sync fromstart
au BufNewFile *.html silent! 0r $HOME/.vim/templates/tpl.html
au FileType go,c,cpp,java setlocal noet sw=4 ts=4

let g:netrw_banner       = 0
let g:netrw_browse_split = 0
let g:netrw_cursor       = 0
let g:netrw_cursorline   = 0
let g:netrw_dirhistmax   = 0
let g:netrw_fastbrowse   = 1
let g:netrw_keepdir      = 1
let g:netrw_list_hide    = '*.db,desktop.ini,\$Recycle\.Bin,NTUSER\.*,\.DS_Store,\.\/$'
let g:netrw_liststyle    = 0
let g:netrw_menu         = 0
let g:netrw_winsize      = 24

let g:plug_window = "new"

colo less
au! GuiEnter * silent set visualbell t_vb=""
if has("gui_running")
  set go=""
  if has("mac")
      set guifont=Menlo:h14
  elseif has("unix")
      set guifont=Input\ 11
  elseif has("win32")
    set columns=120 lines=45
    set guifont=Consolas:h12
  endif
endif
