" execute pathogen#infect()

call plug#begin()
Plug 'ervandew/supertab'
Plug 'jiangmiao/auto-pairs'
Plug 'justinmk/vim-sneak'
Plug 'tomtom/tcomment_vim'
Plug 'editorconfig/editorconfig-vim'
Plug 'fatih/vim-go', { 'for': 'go' }
Plug 'pangloss/vim-javascript', { 'for': 'javascript' }
Plug 'vim-scripts/HTML-AutoCloseTag', { 'for': 'html' }
Plug 'tikhomirov/vim-glsl', { 'for': 'glsl' }
" Plug 'lambdatoast/elm.vim', { 'for': 'elm' }
Plug 'duganchen/vim-soy', { 'for': 'soy' }
call plug#end()

syntax on
filetype plugin indent on
let mapleader = "\<space>"

set autoread hidden
set shiftwidth=4 tabstop=4 backspace=indent,eol,start
set scrolloff=2
set expandtab
set smartcase incsearch
set guicursor=n:blinkon0
set lazyredraw
set mouse=a mousemodel=extend
set number ls=2
set omnifunc=syntaxcomplete#Complete
set shortmess=atIc
set showtabline=1
set ttyfast ttyscroll=3
set wildmenu wildmode=longest,list,full
set path+=**
set noerrorbells vb t_vb=""
set nowritebackup noswapfile viminfo="NONE"
set noshowmatch
set nostartofline
set nowrap
set nofoldenable

runtime macros/matchit.vim

""" mappings
nn j gj
nn k gk
inoremap jj <esc>

vn < <gv
vn > >gv
nm <silent> <leader>b :ls<cr>:e #
nm <silent> <leader>e :Explore<cr>
nn <silent> <leader>q :call wipeout#Wipeout()<cr>
nn  <F2> :set invnumber<cr>
nn  <F3> :if exists("g:syntax_on")<Bar>syntax off<Bar>else<Bar>syntax enable<Bar>endif<cr>

noremap  <F1> :set hls!<cr>
inoremap <F1> <nop>

" strip trailing whitespace on save
au BufWritePre * :%s/\s\+$//e
au FileType json,ruby,vim setlocal expandtab tabstop=2 shiftwidth=2
au FileType go setlocal noexpandtab shiftwidth=8 tabstop=8

" netrw settings
let g:netrw_banner       = 0
let g:netrw_list_hide    = '*.db,desktop.ini,\.DS_Store,\.\/$'
let g:netrw_dirhistmax   = 0
let g:netrw_keepdir      = 1
let g:netrw_cursorline   = 0
let g:netrw_cursor       = 0
let g:netrw_browse_split = 0
let g:netrw_winsize      = 24
let g:netrw_cursorline   = 0
let g:netrw_cursor       = 0
let g:netrw_fastbrowse   = 2
let g:netrw_browse_split = 0

" vim-go
let g:go_fmt_autosave =                        1
let g:go_fmt_command =                         "goimports"
let g:go_highlight_array_whitespace_error =    0
let g:go_highlight_space_tab_error =           0
let g:go_highlight_trailing_whitespace_error = 0

colo jellybeans

" gvim specific settings
au! GuiEnter * silent set visualbell t_vb=""
if has("gui_running")
  set lines=40 columns=110
  set guitablabel=%M%t
  set guioptions-=T
  set guioptions-=e
  set guioptions-=m
  set guioptions-=r
  if has("unix")
    if has("mac")
      set guifont=Menlo:h14
    else
      set guifont=Droid\ Sans\ Mono\ 11
    endif
  elseif has("win32")
    set guifont=Consolas:h12
  endif
endif

" colors
hi Visual term=none ctermfg=White gui=reverse guifg=White
